# Copy this file to backend/.env and fill in your key.
#
# The hosted vision-language model is called through the official `openai`
# client, which reads this variable itself. The backend will not start without
# it: scans/pipeline/vlm.py builds the client at import time.
OPENAI_API_KEY=sk-your_api_key_here
